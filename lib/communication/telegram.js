const Telegraf = require('telegraf')

const start = () => {
    // const bot = new Telegraf(process.env.BOT_TOKEN)
    // bot.start((ctx) => ctx.reply('Welcome!'))
    // bot.start((ctx) => ctx.reply('Password plz 🔑'))
    // bot.help((ctx) => ctx.reply('Control your pixel-grid with me. More on https://github.com/schmuto/pixel-box.'))

    // bot.hears('gif', (ctx) => ctx.reply('3frames.gif'))
    // bot.launch()
}

module.exports = {start};